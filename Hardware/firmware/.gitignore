build/
.vscode/
*.uf2
*.elf
*.bin
*.hex
*.dis
*.map
__pycache__/
*.pyc
*.csv.out

# Bench captures from tools/scope.py. Auto-named runs land in captures/;
# a long session writes dozens of these and they are raw data, not source.
# Anything you name yourself with --csv is deliberate and is NOT ignored.
captures/
roll_*.csv
